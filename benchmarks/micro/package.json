{
  "name": "micro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p tmp && browserify index.js -o ./tmp/bench.js ",
    "watch": "mkdir -p tmp && watchify index.js -o ./tmp/bench.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "benchmark": "^1.0.0",
    "browserify": "^6.3.3",
    "watchify": "^2.1.1"
  }
}
