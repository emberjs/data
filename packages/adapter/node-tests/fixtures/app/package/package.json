{
  "name": "my-app",
  "version": "0.0.0",
  "description": "App fixture package for ember-cli-blueprint-test-helpers",
  "private": true,
  "engines": {
    "node": "^14.8.0 || 16.* || >= 18.*",
    "yarn": "use pnpm",
    "npm": "use pnpm",
    "pnpm": "7.6.0"
  },
  "devDependencies": {
    "ember-cli": "*",
    "ember-data": "*"
  }
}
