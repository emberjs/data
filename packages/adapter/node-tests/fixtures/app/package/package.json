{
  "name": "my-app",
  "version": "0.0.0",
  "description": "App fixture package for ember-cli-blueprint-test-helpers",
  "private": true,
  "engines": {
    "node": "10.* || >= 12.*"
  },
  "devDependencies": {
    "ember-cli": "*",
    "ember-data": "*"
  }
}
