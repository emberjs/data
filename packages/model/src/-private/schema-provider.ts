import { SchemaService } from '@ember-data/types/q/schema-service';

// FIXME: Implement or remove invocations
export function buildSchema(store: unknown): SchemaService {
  throw new Error('unimplemented');
}
