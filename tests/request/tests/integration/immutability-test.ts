import { module, test } from 'qunit';

module('RequestManager | Immutability', function () {
  test('RequestInfo passed to a handler is Immutable', function (assert) {
    assert.ok(false, 'Not Implemented');
  });
});
