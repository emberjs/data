{
  "name": "my-app",
  "version": "0.0.0",
  "description": "App fixture package for ember-cli-blueprint-test-helpers",
  "private": true,
  "engines": {
    "node": "^14.8.0 || 16.* || >= 18.*"
  },
  "devDependencies": {
    "ember-cli": "*",
    "ember-data": "*",
    "@ember-data/serializer": "*"
  }
}
