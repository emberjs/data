'use strict';

module.exports = function (environment) {
  let ENV = {
    modulePrefix: 'full-data-asset-size-app',
    environment,
    rootURL: '/',
    locationType: 'history',
    EmberENV: {
      FEATURES: {},
      EXTEND_PROTOTYPES: {},
    },

    APP: {},
  };

  return ENV;
};
